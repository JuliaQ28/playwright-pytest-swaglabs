[pytest]
pythonpath = .
addopts = --html=reports/report.html --self-contained-html
markers =
    smoke: critical-path tests that must pass on every commit (fast subset)
    regression: full regression suite (all tests, including smoke)
